Next steps:

1. Copy the public ip generated for service "{{ .Release.Name }}-services-lb", and set it as the url in the Digma's IDE plugin (IntelliJ) settings.

2. Add Digma's collector endpoint "http://{{ tpl .Values.digmaCollectorApi.host . }}.{{.Release.Namespace}}:5050" in your app or OTLP collector config file.
{{ if .Values.embeddedJaeger.enabled }}
3. Add Jaeger's endpoint "http(or https)://<your LB public ip address>:17686" to Digma's IDE 'Jaeger Query URL(for embedded mode)' settings.
{{ end }}
